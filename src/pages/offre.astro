---
import BaseLayout from '../layouts/BaseLayout.astro';
import Card from '../components/Card.astro';
import StepCard from '../components/StepCard.astro';
import PricingCard from '../components/PricingCard.astro';
import { site, beneficesOffre, etapesCommentCaMarche, formules } from '../data/site';
---

<BaseLayout title="L'offre" description="Découvrez les formules de batchcooking à domicile de La mère Simone.">
  <section class="intro">
    <div class="container intro-grid">
      <div class="intro-text">
        <h1>{site.tagline}</h1>
        <p>
          Je viens cuisiner chez vous tous vos repas de la semaine ! Vous choisissez votre formule et je
          m&rsquo;occupe du reste
        </p>
        <p class="secteur">Secteur {site.secteur}</p>
      </div>

      <img class="intro-photo" src="/images/photo-offre.jpg" alt="Lola, La mère Simone" />

      <div class="intro-badges">
        {
          beneficesOffre.map((benefice) => (
            <Card variant="mauve">
              {
                benefice.texte.split('\n').map((ligne, i) => (
                  <>
                    {i > 0 && <br />}
                    {ligne}
                  </>
                ))
              }
            </Card>
          ))
        }
      </div>
    </div>
  </section>

  <section class="comment-ca-marche">
    <div class="container">
      <h2>Comment ça marche ?</h2>
      <div class="steps-grid">
        {etapesCommentCaMarche.map((etape) => <StepCard {...etape} />)}
      </div>
    </div>
  </section>

  <section class="formules">
    <div class="container">
      <h2>Les formules</h2>
      <div class="formules-grid">
        {formules.map((formule) => <PricingCard formule={formule} />)}
      </div>
      <a class="btn btn-block devis-cta" href="/contact">Demander mon devis</a>
    </div>
  </section>
</BaseLayout>

<style>
  .intro {
    background: var(--color-cream);
    padding-block: var(--space-section);
  }

  .intro-grid {
    display: grid;
    grid-template-columns: 1.1fr 0.9fr auto;
    gap: 2.75rem;
    align-items: start;
  }

  .intro-text h1 {
    font-size: clamp(2.5rem, 4vw, 3.5rem);
    margin-bottom: 1.35rem;
  }

  .intro-text p {
    font-size: 1.1rem;
    line-height: 1.6;
    max-width: 34ch;
    margin-bottom: 1.1rem;
    opacity: 0.92;
  }

  .secteur {
    font-style: italic;
    opacity: 0.75;
  }

  .intro-photo {
    border-radius: var(--radius-lg);
    align-self: center;
    box-shadow: var(--shadow-md);
    transition: transform var(--transition), box-shadow var(--transition);
  }

  .intro-photo:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
  }

  .intro-badges {
    display: flex;
    flex-direction: column;
    gap: 1.1rem;
  }

  .comment-ca-marche {
    background: var(--color-olive);
    color: var(--color-cream);
    padding-block: var(--space-section);
  }

  .comment-ca-marche h2 {
    font-size: clamp(2.1rem, 4vw, 2.85rem);
    margin-bottom: 2.75rem;
  }

  .steps-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.75rem;
  }

  .formules {
    background: var(--color-cream);
    padding-block: var(--space-section);
    text-align: center;
  }

  .formules h2 {
    font-size: clamp(2.1rem, 4vw, 2.85rem);
    margin-bottom: 2.75rem;
  }

  .formules-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 320px));
    gap: 1.75rem;
    justify-content: center;
    margin-bottom: 2.5rem;
    text-align: left;
  }

  .devis-cta {
    max-width: 360px;
    margin-inline: auto;
  }

  @media (max-width: 900px) {
    .intro-grid {
      grid-template-columns: 1fr;
    }

    .steps-grid,
    .formules-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
